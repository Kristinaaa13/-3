# 3) Цикл у зв’язаному списку (Флойд: черепаха/заєць)
def hasCycle(head: Optional[ListNode]) -> bool:
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow is fast:  # вказують на один вузол
            return True
    return False
# Складність: O(n) / O(1).
